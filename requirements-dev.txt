# Auditoría de SEGURIDAD pre-deploy de Escriba (regla pre-deploy del ecosistema).
# Nota: el gate de calidad completo (ruff/vulture/mypy) sigue siendo iniciativa aparte.
# Ya hay una suite mínima de tests (arranca por la lógica fail-safe: auth + anti-SSRF; ver tests/).
bandit[toml]>=1.9,<2.0     # SAST (gateado a Medium+; los Low de subprocess son inherentes)
pip-audit>=2.10,<3.0       # vulnerabilidades conocidas en dependencias (OSV/CVE)
detect-secrets>=1.5,<2.0   # que no se filtre ningún secreto al repo
pytest>=8.0,<9.0           # suite de tests (auth + anti-SSRF; anonimal/anon_rules requieren re2)
